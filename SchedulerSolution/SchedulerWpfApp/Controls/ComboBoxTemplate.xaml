<UserControl x:Class="SchedulerWpfApp.Controls.ComboBoxTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SchedulerWpfApp.Controls"
             mc:Ignorable="d">
    <ComboBox x:Name="ComboBox"
              ItemsSource="{Binding FilteredItems, RelativeSource={RelativeSource AncestorType=UserControl}}"
              SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
              Text="{Binding SearchText, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"
              IsEditable="True"
              SelectionChanged="ComboBox_SelectionChanged"
              IsTextSearchEnabled="False"
              StaysOpenOnEdit="True"
              Padding="4"
              BorderThickness="1"
              BorderBrush="#CCC"
              VerticalContentAlignment="Center"
              Background="White"
              Height="30" />
</UserControl>