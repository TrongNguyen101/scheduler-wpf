<UserControl x:Class="SchedulerWpfApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Background="#F7FAFF">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="1" Width="360" Padding="16" Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1" HorizontalAlignment="Center" >
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Đăng nhập" FontSize="20" FontWeight="Bold" Foreground="#111827"/>
                    <StackPanel Grid.Row="1" Margin="0,8,0,0">
                        <TextBlock Text="Tài khoản" FontSize="12" Foreground="#6B7280"/>
                        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Margin="0,8,0,0">
                        <TextBlock Text="Mật khẩu" FontSize="12" Foreground="#6B7280"/>
                        <PasswordBox x:Name="Pwd" PasswordChar="*"/>
                    </StackPanel>
                    <StackPanel Grid.Row="3" Margin="0,8,0,0">
                        <TextBlock Text="{Binding Error}" Foreground="#DC2626"/>
                        <Button Content="Đăng nhập" Margin="0,8,0,0" Background="#4F46E5" Foreground="White" Command="{Binding LoginCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>


